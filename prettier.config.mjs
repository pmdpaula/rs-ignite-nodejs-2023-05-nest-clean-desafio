const config = {
  trailingComma: "all",
  useTabs: false,
  tabWidth: 2,
  semi: true,
  singleQuote: false,
  printWidth: 90,
  arrowParens: "always",
  endOfLine: "lf",
  singleAttributePerLine: true,
  plugins: [
  ],
};

export default config;
